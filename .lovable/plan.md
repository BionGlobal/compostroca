

# Substituir favicon.ico

Copiar o arquivo enviado para `public/favicon.ico` e atualizar o `index.html` para referenciar `/favicon.ico`.

### Alterações:
1. **Copiar arquivo**: `user-uploads://favicon.ico` → `public/favicon.ico`
2. **`index.html`**: Atualizar `<link rel="icon">` para `href="/favicon.ico" type="image/x-icon"`

